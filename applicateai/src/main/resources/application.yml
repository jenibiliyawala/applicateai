## YAML Template.
---
server:
  port : 3010

spring:
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    password:
    url: jdbc:mysql://localhost/applicateai
    username: root
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update